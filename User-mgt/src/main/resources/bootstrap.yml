spring:
  application:
    name: user-mgt
  cloud:
    nacos:
      config:
        server-addr: localhost:8848
        extension-configs:
          - config:
            data-id: user-mgt
            refresh: true
        file-extension: yaml
